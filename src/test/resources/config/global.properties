# Framework defaults. Environment files (dev/stag/prod.properties) override these,
# then config/local.properties (git-ignored), then environment variables, then -D flags.

browser=chrome
headless=false
wait.explicit.seconds=15
wait.pageload.seconds=60
screenshot.every.step=false

# Selenium Grid / cloud hub. Leave empty to run browsers locally.
# grid.url=http://localhost:4444
